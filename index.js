// Точка входа приложения на Expo Router.
// Проект использует файловую маршрутизацию (каталог app/), поэтому корневой
// компонент регистрируется через expo-router/entry, а не через ./App.
import 'expo-router/entry';
