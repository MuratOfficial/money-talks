# 🎨 Анимация загрузки - Документация

## Обзор

Добавлена универсальная анимация загрузки для всех компонентов, которые делают запросы на сервер.

## 📦 Компонент LoadingAnimation

### Расположение
```
app/components/LoadingAnimation.tsx
```

### Возможности
- ✅ Полноэкранный режим
- ✅ Компактный режим для встраивания
- ✅ Поддержка светлой/темной темы
- ✅ Градиентный дизайн
- ✅ Настраиваемое сообщение
- ✅ Анимированные точки

### Использование

#### Полноэкранный режим
```tsx
import LoadingAnimation from '@/app/components/LoadingAnimation';

if (loading) {
  return (
    <View className="flex-1 bg-white">
      <LoadingAnimation 
        fullScreen 
        message="Загрузка данных..." 
      />
    </View>
  );
}
```

#### Компактный режим
```tsx
<LoadingAnimation 
  message="Загрузка..." 
  size="small"
/>
```

### Props

| Prop | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `message` | string | "Загрузка..." | Текст сообщения |
| `size` | 'small' \| 'large' | 'large' | Размер индикатора |
| `fullScreen` | boolean | false | Полноэкранный режим |

## 📊 Интегрированные компоненты

### 1. **InvestmentsPage** (`app/main/invest/index.tsx`)

**API вызовы:**
- `fetchQuestions()` - Загрузка вопросов теста
- `fetchTips()` - Загрузка подсказок

**Анимация:**
```tsx
if (loading) {
  return (
    <View className={`flex-1 ${bgColor}`}>
      <LoadingAnimation 
        fullScreen 
        message="Загрузка вопросов теста..." 
      />
    </View>
  );
}
```

**Когда показывается:**
- При первой загрузке страницы
- При получении вопросов с сервера

---

### 2. **GoalsScreen** (`app/main/goals/main.tsx`)

**API вызовы:**
- `fetchTips('incomes')` - Загрузка подсказок

**Анимация:**
```tsx
if (loading) {
  return (
    <SafeAreaView className={`flex-1 ${bgColor}`}>
      <LoadingAnimation 
        fullScreen 
        message="Загрузка подсказок..." 
      />
    </SafeAreaView>
  );
}
```

**Когда показывается:**
- При первой загрузке страницы целей
- При получении подсказок с сервера

---

### 3. **PageComponent** (`app/components/PageComponents.tsx`)

**API вызовы:**
- `fetchTips('incomes')` - Загрузка подсказок

**Анимация:**
```tsx
if (loading) {
  return (
    <SafeAreaView className={`flex-1 ${bgColor}`}>
      <LoadingAnimation 
        fullScreen 
        message="Загрузка подсказок..." 
      />
    </SafeAreaView>
  );
}
```

**Используется в:**
- Доходы
- Расходы
- Активы
- Пассивы
- Анализ

---

### 4. **ChatGPTFeature** (`app/components/ChatGPTFeature.tsx`)

**API вызовы:**
- `sendChatGPTMessage()` - Отправка сообщений AI

**Анимация:**
```tsx
{isLoading && (
  <View className="items-start mb-4">
    <View className="bg-gray-700 p-3 rounded-2xl">
      <View className="flex-row items-center">
        <Animated.View style={{ transform: [{ scale: pulseAnim }] }}>
          <Ionicons name="ellipsis-horizontal" size={16} color="#F97316" />
        </Animated.View>
        <Text className="text-gray-300 text-sm ml-2">
          AI думает...
        </Text>
      </View>
    </View>
  </View>
)}
```

**Особенность:**
- Кастомная анимация пульсации
- Показывается во время ответа AI

---

## 🎨 Дизайн

### Цвета

**Светлая тема:**
- Градиент: `#4CAF50` → `#81C784`
- Фон: Белый
- Текст: `#111827`

**Темная тема:**
- Градиент: `#4CAF50` → `#2196F3`
- Фон: Черный
- Текст: Белый

### Размеры

**Полноэкранный режим:**
- Круг: 100x100px
- Тень: 8px
- Отступы: Центрирование

**Компактный режим:**
- Круг: 60x60px
- Тень: 4px
- Отступы: 8px вертикально

## 🔄 Состояния загрузки

### Типичный flow

```typescript
const [loading, setLoading] = useState(true);

const loadData = async () => {
  try {
    setLoading(true);
    const data = await fetchData();
    // Обработка данных
  } catch (error) {
    console.error('Error:', error);
  } finally {
    setLoading(false);
  }
};
```

## 📱 Примеры использования

### Пример 1: Простая загрузка

```tsx
const MyComponent = () => {
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    loadData();
  }, []);

  const loadData = async () => {
    try {
      setLoading(true);
      await fetchSomeData();
    } finally {
      setLoading(false);
    }
  };

  if (loading) {
    return <LoadingAnimation fullScreen message="Загрузка..." />;
  }

  return <View>{/* Контент */}</View>;
};
```

### Пример 2: Загрузка с ошибкой

```tsx
const MyComponent = () => {
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  if (loading) {
    return <LoadingAnimation fullScreen message="Загрузка..." />;
  }

  if (error) {
    return (
      <View className="flex-1 items-center justify-center">
        <Text className="text-red-500">{error}</Text>
      </View>
    );
  }

  return <View>{/* Контент */}</View>;
};
```

### Пример 3: Встроенная загрузка

```tsx
<ScrollView>
  {loading ? (
    <LoadingAnimation message="Загрузка списка..." />
  ) : (
    items.map(item => <ItemCard key={item.id} {...item} />)
  )}
</ScrollView>
```

## ✨ Преимущества

1. **Единообразие** - Одинаковый стиль во всем приложении
2. **Переиспользование** - Один компонент для всех случаев
3. **Адаптивность** - Поддержка светлой/темной темы
4. **Гибкость** - Полноэкранный и компактный режимы
5. **UX** - Пользователь всегда знает, что происходит

## 🎯 Лучшие практики

### ✅ DO

```tsx
// Показывать загрузку при первой загрузке
if (loading) {
  return <LoadingAnimation fullScreen message="Загрузка..." />;
}

// Использовать осмысленные сообщения
<LoadingAnimation message="Загрузка вопросов теста..." />

// Всегда устанавливать loading в false
finally {
  setLoading(false);
}
```

### ❌ DON'T

```tsx
// Не показывать загрузку без сообщения
<LoadingAnimation />

// Не забывать сбрасывать loading
await fetchData();
// setLoading(false); ← Забыли!

// Не использовать несколько разных индикаторов
<ActivityIndicator /> // Используйте LoadingAnimation
```

## 📊 Метрики

### Компоненты с анимацией загрузки

| Компонент | API вызовы | Статус |
|-----------|------------|--------|
| InvestmentsPage | fetchQuestions, fetchTips | ✅ |
| GoalsScreen | fetchTips | ✅ |
| PageComponents | fetchTips | ✅ |
| ChatGPTFeature | sendChatGPTMessage | ✅ (кастомная) |
| TestComponent | - | ⚠️ Данные передаются |

### Покрытие

- **Компонентов с API**: 4
- **С LoadingAnimation**: 3
- **С кастомной анимацией**: 1
- **Покрытие**: 100% ✅

## 🔮 Будущие улучшения

- [ ] Скелетон-загрузка для списков
- [ ] Прогресс-бар для длительных операций
- [ ] Анимация успешной загрузки
- [ ] Retry функционал при ошибке
- [ ] Кэширование для уменьшения загрузок

---

**Дата создания**: 2025-12-20  
**Статус**: ✅ Реализовано  
**Покрытие**: 100% компонентов с API вызовами
