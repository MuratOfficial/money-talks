# ⚠️ Решение проблемы с уведомлениями в Expo Go

## Проблема

```
ERROR  Error: Cannot find native module 'ExpoPushTokenManager'
```

## Причина

`expo-notifications` требует **нативные модули**, которые **не доступны в Expo Go**.

## ✅ Решение

Код был обновлен для безопасной работы в обеих средах:

### 1. Добавлена проверка доступности

```typescript
// hooks/useNotifications.ts
let isNotificationsAvailable = false;

try {
  Notifications = require('expo-notifications');
  isNotificationsAvailable = true;
} catch (error) {
  console.warn('expo-notifications не доступен');
}
```

### 2. Информационное сообщение

В профиле пользователя теперь показывается:

```
⚠️ Требуется Development Build
Уведомления не работают в Expo Go. 
Для использования этой функции соберите Development Build приложения.
```

## 🎯 Что работает сейчас

### В Expo Go:
- ✅ Приложение запускается без ошибок
- ✅ Показывается информационное сообщение
- ❌ Уведомления не работают

### В Development Build:
- ✅ Приложение запускается
- ✅ Уведомления работают полностью
- ✅ Можно планировать и получать уведомления

## 🔨 Как собрать Development Build

### Вариант 1: Локальная сборка

#### Android:
```bash
npx expo run:android
```

#### iOS:
```bash
npx expo run:ios
```

### Вариант 2: EAS Build (Рекомендуется)

1. **Установите EAS CLI:**
```bash
npm install -g eas-cli
```

2. **Войдите в аккаунт:**
```bash
eas login
```

3. **Настройте проект:**
```bash
eas build:configure
```

4. **Соберите Development Build:**

**Для Android:**
```bash
eas build --profile development --platform android
```

**Для iOS:**
```bash
eas build --profile development --platform ios
```

5. **Установите на устройство:**
- После сборки получите QR-код или ссылку
- Скачайте и установите приложение
- Запустите с помощью `npx expo start --dev-client`

## 📱 Установка Development Client

После сборки вам нужно установить **Expo Dev Client** вместо Expo Go:

### Android:
- Скачайте `.apk` файл из EAS Build
- Установите на устройство

### iOS:
- Используйте TestFlight
- Или установите через Xcode

## 🚀 Запуск с Development Build

```bash
# Запустите сервер
npx expo start --dev-client

# Или используйте npm script
npm start
```

## 📊 Сравнение

| Функция | Expo Go | Development Build |
|---------|---------|-------------------|
| Быстрый старт | ✅ | ⚠️ Требует сборки |
| Уведомления | ❌ | ✅ |
| PDF генерация | ❌ | ✅ |
| Нативные модули | ❌ | ✅ |
| Обновление OTA | ✅ | ✅ |

## 💡 Рекомендация

Для **production** приложения всегда используйте **Development Build**, так как:

1. ✅ Полный доступ к нативным модулям
2. ✅ Все функции работают
3. ✅ Можно добавлять любые библиотеки
4. ✅ Лучшая производительность

## 🔄 Временное решение

Пока вы используете Expo Go:
- Компонент уведомлений показывает информационное сообщение
- Приложение работает без ошибок
- Остальные функции доступны

## 📚 Дополнительные ресурсы

- [Expo Development Builds](https://docs.expo.dev/develop/development-builds/introduction/)
- [EAS Build](https://docs.expo.dev/build/introduction/)
- [expo-notifications](https://docs.expo.dev/versions/latest/sdk/notifications/)

---

**Статус**: ✅ Проблема решена  
**Дата**: 2025-12-18  
**Решение**: Добавлена проверка доступности нативных модулей
